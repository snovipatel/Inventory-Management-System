# Inventory-Management-System
A console-based application that manages electronic devices and apparel. Features include loading inventory from a file, displaying detailed item information, searching by name, selling and restocking with validation, all built with object-oriented design using abstract classes and interfaces.
